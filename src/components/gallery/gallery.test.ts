import Gallery from "./gallery";
/* import {GalleryTest} from "./gallery";

describe('Gallery tests', () => {
  describe('Get the gallery photos by page',  () => {
    it('Should fail if we request a page that doesnt exists', async () => {
        const result = await GalleryTest.loadNextPage(-1)
        expect(result).toBe("KO");
    })

    it('Should return a list of photos', async () => {
      
      const result = await GalleryTest.loadNextPage({page:1})
      expect(result).toBe("OK");
      
    })
  })
}); */